// 网页版

export const saveJsonPromise = () => {
  return Promise.resolve();
};

export const readJsonPromise = () => {
  return Promise.resolve();
};

export const getUserConfig = () => {
  return Promise.resolve();
};
